function minSwaps(grid: number[][]): number {
  let result = true;
  let cnt = 0;
  while (result) {}
}

const judgeValid = (grid: number[][]) => {
  for (let index = 0; index < grid.length; index++) {
    const row = grid[index].slice(index + 1);
  }
};
